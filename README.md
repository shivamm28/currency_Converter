# currency_Converter
a simple react app used to implement currency conversions. It uses custom react hooks and default react hooks for UI updation and state-presentation. Also uses a free currency API to fetch currency conversion rates.
